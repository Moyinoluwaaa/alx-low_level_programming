#include "main.h"

/**
* _isalpha - the func name
* @c: the parameter
*
* Return: ends the program
*/
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
		return (1);
	else
		return (0);
return (0);
}
