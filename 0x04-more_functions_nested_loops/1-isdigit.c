#include "main.h"
/**
 * _isdigit - func name
 * @c: param name
 *
 * Return: ends
 */
int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
	{
		return (1);
	}
	else
	{
		return (0);
	}
putchar('\n');
return (0);
}
