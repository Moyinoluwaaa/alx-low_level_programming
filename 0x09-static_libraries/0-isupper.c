#include "main.h"
/**
 * _isupper - func name
 * @c: parameter
 *
 * Return: ends
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
putchar('\n');
return (0);
}
